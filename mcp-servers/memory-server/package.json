{
  "name": "mcp-memory-client",
  "version": "2.0.0",
  "description": "MCP client that bridges browser extension to MCP servers",
  "main": "build/mcp-client.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/mcp-client.js",
    "dev": "tsc && node build/mcp-client.js",
    "clean": "rm -rf build",
    "rebuild": "npm run clean && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.21",
    "typescript": "^5.0.0"
  },
  "keywords": ["mcp", "client", "memory", "bridge"],
  "author": "MCP Development Team",
  "license": "MIT"
}
